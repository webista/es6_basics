/**
 * Template literals
 */
console.groupCollapsed("%cTemplate literals", "font-weight: bold");
let desc = `Like this one`;
console.log(desc);
let descMultiline = `
Or this 
multiline

`;
console.log(descMultiline);

console.groupEnd();
